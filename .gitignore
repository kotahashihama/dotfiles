.DS_Store
Brewfile.old
.qodo

# 非公開層。private_dotfiles.zip で持ち回す
/private/

# 秘匿値の実体（雛形の scripts/secrets.env.example のみ追跡する）
secrets.env
*.env
!scripts/secrets.env.example

# リストア時の退避先
/dotfiles-salvaged-*

# フックが使う textlint。lockfile から npm ci で復元する
home/.claude/hooks/textlint/node_modules/
