version = 1

# 規約そのものが扱う形は、規約の中に現れる。「A ではなく B」を判定する節は
# その形を含まずに書けない。規則ごと切ると他のファイルで見落とすので、
# 検出された文字列を名指しで許す。
[[allow]]
category = "antithesis_repetition"
text = "ではなく B」の1文形も同じ判定です。"
reason = "この形を判定する規約自身の説明。例を含まずに書けない"
